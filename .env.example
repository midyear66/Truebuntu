# Truebuntu Configuration
# REQUIRED: Generate with `openssl rand -hex 24` — app will refuse to start without this
SECRET_KEY=
DATABASE_PATH=/data/nas.db
LOG_LEVEL=info
# Seconds between dashboard snapshot collections (collector idles when nobody is watching)
DASHBOARD_INTERVAL=5
